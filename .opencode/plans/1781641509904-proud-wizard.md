# Plan: Fix sync-scripts — merge opencode.json instead of overwriting it

## Problem

`sync-skills.ps1` (line 90) and `sync-skills.sh` (line 91) both do a hard copy of the repo's `opencode.json` over the global `~/.config/opencode/opencode.json`. Any keys present in the global file but absent from the repo file — such as the `plugin` array — are silently removed on every sync.

Concrete example: global config has `"plugin": ["@tarquinen/opencode-dcp@latest"]`; after sync it is gone.

## Solution

Replace the hard copy with a **deep merge**: read the existing global `opencode.json`, merge the repo's `opencode.json` into it (repo wins on conflicts), and write the result back. Keys present only in the global file are preserved.

### Merge semantics

- **Objects**: deep-merged — repo wins on key conflicts, global keeps non-conflicting keys
- **Arrays**: repo replaces global (arrays are not unioned)
- **Key only in global**: preserved as-is
- **Key only in repo**: added
- **Global file missing**: treated as `{}` — repo's content written fresh

### New file: `scripts/merge-json.js`

A small Node.js helper (node is already a hard dependency of both sync scripts):

```js
// merge-json.js <target-path> <source-path>
// Deep-merges source into target (source wins on conflicts).
// Keys in target absent from source are preserved. Arrays are replaced.
// Writes result back to target.

const fs = require('fs');
const [, , targetPath, sourcePath] = process.argv;

function deepMerge(target, source) {
  const result = { ...target };
  for (const key of Object.keys(source)) {
    const tv = target[key], sv = source[key];
    if (sv !== null && typeof sv === 'object' && !Array.isArray(sv) &&
        tv !== null && typeof tv === 'object' && !Array.isArray(tv)) {
      result[key] = deepMerge(tv, sv);
    } else {
      result[key] = sv;
    }
  }
  return result;
}

let existing = {};
try { existing = JSON.parse(fs.readFileSync(targetPath, 'utf8')); } catch (_) {}
const incoming = JSON.parse(fs.readFileSync(sourcePath, 'utf8'));
fs.writeFileSync(targetPath, JSON.stringify(deepMerge(existing, incoming), null, 2) + '\n');
```

### `scripts/sync-skills.ps1` change

Replace the `Copy-Item opencode.json` block with a `node merge-json.js` call:

```powershell
$OpenCodeJson = Join-Path $RepoRoot 'opencode.json'
if (Test-Path -LiteralPath $OpenCodeJson) {
    $GlobalOpenCodeJson = Join-Path $GlobalConfig 'opencode.json'
    $MergeScript = Join-Path $RepoRoot 'scripts\merge-json.js'
    node $MergeScript $GlobalOpenCodeJson $OpenCodeJson
    Write-Host "  merged  opencode.json -> $GlobalConfig\opencode.json"
} else {
    Write-Host "  WARN: opencode.json not found, skipping"
}
```

### `scripts/sync-skills.sh` change

Replace the `cp opencode.json` block with a `node merge-json.js` call:

```bash
if [[ -f "$REPO_ROOT/opencode.json" ]]; then
  node "$REPO_ROOT/scripts/merge-json.js" "$GLOBAL_CONFIG/opencode.json" "$REPO_ROOT/opencode.json"
  echo "  merged  opencode.json -> $GLOBAL_CONFIG/opencode.json"
else
  echo "  WARN: opencode.json not found, skipping"
fi
```

## Files to modify

| File | Action |
|------|--------|
| `scripts/merge-json.js` | **Create** — deep-merge helper |
| `scripts/sync-skills.ps1` | **Modify** — replace `Copy-Item opencode.json` with `node merge-json.js` call |
| `scripts/sync-skills.sh` | **Modify** — replace `cp opencode.json` with `node merge-json.js` call |

## Acceptance Criteria

- [ ] `scripts/merge-json.js` exists and deep-merges two JSON files (source into target, source wins)
- [ ] Running `sync-skills.ps1` with a global `opencode.json` that has a `plugin` key preserves that `plugin` key after the sync
- [ ] Running `sync-skills.ps1` with a global `opencode.json` that has no `permission` key adds the repo's `permission` block
- [ ] Running `sync-skills.ps1` when global `opencode.json` does not exist writes the repo's content cleanly (no crash)
- [ ] `sync-skills.sh` has the same behaviour as `sync-skills.ps1` for all three cases above

## Verification

1. **Preserve plugin**: run `.\scripts\sync-skills.ps1`; confirm `~/.config/opencode/opencode.json` still contains `"plugin": ["@tarquinen/opencode-dcp@latest"]`
2. **Merge permission**: temporarily add a new permission rule to the global file; run sync; confirm the new rule survives alongside the repo's rules
3. **Missing global file**: rename global `opencode.json` temporarily; run sync; confirm it is created with the repo's content and no error
