# AFK Loop — Fix external_directory Permission for Global STANDARDS.md

## Root Cause

`~/.config/opencode/STANDARDS.md` is outside the repo working directory. OpenCode's `external_directory` permission is triggered for any tool touching a path outside the working directory, and its default is `"ask"`. This causes an approval prompt even when the bash command itself is allowed — breaking AFK.

The previous fix added the bash allowlist entry for `cat ~/.config/opencode/STANDARDS.md` in reviewer-standards but omitted the `external_directory` rule that is required in both agents.

## Implementation Approach

Add `external_directory: { "~/.config/opencode/*": "allow" }` to the permission block of both agents that access the global config.

Also widen the bash pattern in reviewer-standards from `"cat ~/.config/opencode/STANDARDS.md": allow` (exact filename) to `"cat ~/.config/opencode/*": allow` — a small robustness improvement that also covers AGENTS.md if needed.

### Files to change

| File | Change |
|---|---|
| `.opencode/agent/afk-reviewer-standards.md` | Add `external_directory` rule; widen bash cat pattern to `*` |
| `.opencode/agent/afk-orchestrator.md` | Add `external_directory` rule (bash already `"*": allow`) |

### Target permission blocks

**`afk-reviewer-standards.md`**
```yaml
permission:
  edit: deny
  task: deny
  skill: deny
  external_directory:
    "~/.config/opencode/*": allow
  bash:
    "*": deny
    "echo*": allow
    "git diff*": allow
    "git status*": allow
    "cat ~/.config/opencode/*": allow
```

**`afk-orchestrator.md`**
```yaml
permission:
  edit: allow
  task:
    "*": deny
    "afk-*": allow
  skill: allow
  external_directory:
    "~/.config/opencode/*": allow
  bash:
    "*": allow
    "git commit*": deny
    "git push*": deny
    "git branch*": deny
```

---

## Acceptance Criteria

- [x] `afk-reviewer-standards.md` has `external_directory: { "~/.config/opencode/*": allow }`
- [x] `afk-reviewer-standards.md` bash rule uses `"cat ~/.config/opencode/*": allow` (wildcard, not exact filename)
- [x] `afk-orchestrator.md` has `external_directory: { "~/.config/opencode/*": allow }`

---

## Verification Commands

```powershell
Select-String -Path ".opencode/agent/afk-reviewer-standards.md" -Pattern 'external_directory'
Select-String -Path ".opencode/agent/afk-reviewer-standards.md" -Pattern 'cat ~/.config/opencode/\*'
Select-String -Path ".opencode/agent/afk-orchestrator.md" -Pattern 'external_directory'
```
